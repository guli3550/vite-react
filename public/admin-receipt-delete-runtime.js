(() => {
  'use strict';
  const API=()=> (window.__GULI_API_URL||'https://guli-lingerie-api.onrender.com').replace(/\/$/,'');
  const token=()=>sessionStorage.getItem('guli_admin_token')||'';
  const getOrders=async()=>{const r=await fetch(`${API()}/api/admin/orders?limit=500`,{headers:{Authorization:`Bearer ${token()}`},cache:'no-store'});const j=await r.json().catch(()=>({}));if(!r.ok||!j.success)throw new Error(j.message||'Buyurtmalarni olishda xatolik');return Array.isArray(j.data)?j.data:[]};
  const scan=()=>{const table=[...document.querySelectorAll('table')].find(t=>/Kartadan to‘lovlar va Cheklar|CARD PAYMENTS & RECEIPTS/i.test(t.parentElement?.textContent||''));if(!table)return;[...table.querySelectorAll('tbody tr')].forEach(row=>{if(row.dataset.guliReceiptDelete==='1')return;const cells=[...row.children];if(cells.length<9||!row.querySelector('td:first-child img'))return;const tx=[...cells[8].querySelectorAll('button')].find(b=>/Chekni ko‘rish|View receipt|Chek/i.test(b.textContent||''));if(!tx)return;const orderText=String(cells[2]?.textContent||'').trim();const del=document.createElement('button');del.type='button';del.className='dangerBtn miniBtn';del.textContent='🗑 Chekni o‘chirish';del.addEventListener('click',async()=>{if(!confirm(`${orderText||'Ushbu buyurtma'} uchun chekni o‘chirishni tasdiqlaysizmi?`))return;del.disabled=true;del.textContent='⏳ O‘chirilmoqda…';try{const orders=await getOrders();const order=orders.find(o=>String(o.order_number||'').trim()===orderText);if(!order?.id)throw new Error('Buyurtma ID topilmadi');const r=await fetch(`${API()}/api/admin/orders/${encodeURIComponent(order.id)}/payment-receipt`,{method:'DELETE',headers:{Authorization:`Bearer ${token()}`}});const j=await r.json().catch(()=>({}));if(!r.ok||!j.success)throw new Error(j.message||'Chekni o‘chirishda xatolik');const refresh=[...document.querySelectorAll('button')].find(b=>/Yangilash/i.test(b.textContent||''));if(refresh)refresh.click();else location.reload()}catch(e){alert(e instanceof Error?e.message:'Chekni o‘chirishda xatolik');del.disabled=false;del.textContent='🗑 Chekni o‘chirish'}});tx.insertAdjacentElement('afterend',del);row.dataset.guliReceiptDelete='1'})};
  const observer=new MutationObserver(scan);observer.observe(document.documentElement,{childList:true,subtree:true});setTimeout(scan,1200);setInterval(scan,5000);
})();
