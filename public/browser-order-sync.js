(() => {
  'use strict';
  const API='/api'; const ACCESS='guli_access_token';
  const SNAPSHOT='guli_server_orders_snapshot_v2'; const FP='guli_server_orders_fingerprint_v2'; const NOTE='guli_last_order_status_notification_v2'; let running=false;
  const headers=()=>{const h={Accept:'application/json'};const tg=window.Telegram?.WebApp;if(tg?.initData)h['X-Telegram-Init-Data']=tg.initData;else{const token=localStorage.getItem(ACCESS)||'';if(token)h.Authorization=`Bearer ${token}`;}return h;};
  const hasSession=()=>Boolean(window.Telegram?.WebApp?.initData||localStorage.getItem(ACCESS));
  const normalize=r=>({id:String(r.order_number||r.id||''),order_number:r.order_number,first_name:r.first_name,last_name:r.last_name,customer_name:r.customer_name,phone:r.phone||'',items:Array.isArray(r.items)?r.items:[],subtotal:Number(r.subtotal||0),delivery:Number(r.delivery||0),discount:Number(r.discount||0),total:Number(r.total||0),address:r.address,payment:r.payment||'',payment_status:r.payment_status||'pending',payment_receipt_path:r.payment_receipt_path,status:r.status||'⏳ Buyurtma kutilmoqda',createdAt:r.created_at||r.createdAt||new Date().toISOString(),updatedAt:r.updated_at||r.updatedAt,statusUpdatedAt:r.status_updated_at||r.updated_at});
  const fp=o=>(o||[]).map(x=>[x.id,x.status,x.payment_status,x.updatedAt,x.statusUpdatedAt].join('|')).join('||');
  const toast=text=>{let e=document.querySelector('[data-guli-order-sync-toast]');if(!e){e=document.createElement('div');e.dataset.guliOrderSyncToast='1';e.style.cssText='position:fixed;left:50%;bottom:96px;transform:translateX(-50%);z-index:100001;background:#261e20;color:#fff;padding:13px 16px;border-radius:16px;font:700 13px/1.35 system-ui;max-width:calc(100vw - 30px);text-align:center;box-shadow:0 12px 35px rgba(0,0,0,.25)';document.body.appendChild(e)}e.textContent=text;clearTimeout(e._timer);e._timer=setTimeout(()=>e.remove(),2600)};
  async function sync(){if(running||!hasSession())return;running=true;try{const r=await fetch(`${API}/customer/orders`,{headers:headers(),cache:'no-store',credentials:'same-origin'});if(!r.ok)return;const j=await r.json().catch(()=>null);if(!j?.success||!Array.isArray(j.data))return;const current=j.data.map(normalize).filter(x=>x.id);let previous=[];try{previous=JSON.parse(localStorage.getItem(SNAPSHOT)||'[]');if(!Array.isArray(previous))previous=[]}catch{}const oldFp=localStorage.getItem(FP)||'';localStorage.setItem('orders',JSON.stringify(current));localStorage.setItem('guli_orders',JSON.stringify(current));localStorage.setItem(SNAPSHOT,JSON.stringify(current));const newFp=fp(current);if(oldFp&&newFp!==oldFp){const map=new Map(previous.map(x=>[x.id,x]));const changed=current.find(x=>map.get(x.id)&&map.get(x.id).status!==x.status);if(changed){const old=map.get(changed.id);toast(`📦 ${changed.id}: buyurtma holati o‘zgardi — ${changed.status}`);localStorage.setItem(NOTE,JSON.stringify({id:changed.id,from:old.status,to:changed.status,at:new Date().toISOString()}));setTimeout(()=>location.reload(),900);}}else if(!oldFp){setTimeout(()=>location.reload(),250)}localStorage.setItem(FP,newFp)}catch{}finally{running=false}}
  setTimeout(sync,500);setInterval(sync,5000);
})();
